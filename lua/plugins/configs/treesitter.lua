local options = {
  ensure_installed = { "lua", "c", "vimdoc", "bash", "python", "markdown", "markdown_inline" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
